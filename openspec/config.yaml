# OpenSpec config — ChordShift
# Auto-generated by sdd-init on 2026-06-23

context: >
  ChordShift is a mobile-first PWA songbook for guitarists. Vue 3 (Composition API + <script setup>),
  Pinia, Vue Router, Tailwind CSS v4, Vite 8. Pure JavaScript (no TypeScript). Local-first
  persistence via localStorage (songs/playlists) and IndexedDB (audio). PWA with auto-updating
  service worker. UI language: Spanish. Technical artifacts: English.

strict_tdd: false
# Strict TDD is disabled because no test runner (vitest/jest) is configured in the project.
# Install vitest and add unit tests before enabling strict TDD.

testing:
  runner: none
  layers:
    unit: false
    integration: false
    e2e: false
  coverage: false
  linter: false
  type_checker: false
  formatter: false

persistence:
  mode: hybrid
  engram: true
  openspec: true

phases:
  propose:
    required: false
    notes: Optional — skip for trivial changes
  spec:
    required: true
  design:
    required: false
    notes: Optional for well-understood changes
  tasks:
    required: true
  apply:
    required: true
  verify:
    required: true
  archive:
    required: true
